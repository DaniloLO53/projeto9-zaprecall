import React from 'react';
// import PropTypes from 'prop-types';
import styled from 'styled-components';

function QuestionReduced() {
  return (
    <StyledQuestionReducedContainer>
      oi
    </StyledQuestionReducedContainer>
  );
}

const StyledQuestionReducedContainer = styled.div`

`;

export default QuestionReduced;
